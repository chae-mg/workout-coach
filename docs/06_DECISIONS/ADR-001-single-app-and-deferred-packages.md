# ADR-001: 단일 앱 유지와 packages 분리 보류

- 상태: 승인
- 결정일: 2026-09-21

현재 실행 대상은 브라우저 앱 하나이므로 원본 코드는 `apps/web/` 안에 유지한다. 운동 규칙이나 UI를 `packages/`로 미리 분리하지 않는다. 두 번째 앱이나 서버가 같은 모듈을 실제로 사용할 때 공유 패키지로 이동한다.

이 결정은 이동 비용과 import 경로 변화를 줄이고, 실제 공유 경계가 생겼을 때 패키지 책임을 분명히 하기 위한 것이다.
